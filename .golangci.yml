version: "2"

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: false
      rewrite-rules:
        - pattern: interface{}
          replacement: any
    goimports:
      local-prefixes:
        - "github.com/sabafly/gobot"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
  settings:
    unused:
      exported-fields-are-used: true
      generated-is-used: true
      parameters-are-used: true
      local-variables-are-used: true
  exclusions:
    warn-unused: false
    rules:
      - linters:
          - errcheck
        text: "Error return value of `.+\\.Close` is not checked"
      - linters:
          - errcheck
        source: "defer .+$"
